'use client';

/**
 * Client component for now, we could test
 * out revalidate(`/journal/${updatedEntry.id}`)
 * from the upsert in /journal/[id]/route.ts
 */
const Analysis = () => {};

export default Analysis;
